.App {
  text-align: center;
  &-nav {
    &__title {
      display: inline-block;

      padding: 0 10px;
      font-size: 20px;
    }
    &__list {
      display: inline-block;
    }
    .nav-list {
      > li {
        display: inline-block;
        padding: 0 10px;
        &.avtive {
          background: gray;
        }
      }
    }
  }
}
.event{
  background: #000;
  height:100px;
  > div{
    width: 600px;
    height 400px;
  }
}
