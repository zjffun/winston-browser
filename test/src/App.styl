.App {
  &-nav {
    &__title {
      display: inline-block;

      padding: 0 10px;
      font-size: 20px;
    }
    &__list {
      display: inline-block;
    }
    .nav-list {
      > li {
        display: inline-block;
        padding: 0 10px;
        &.avtive {
          background: gray;
        }
      }
    }
  }
}
.event{
  > div{
    height 400px;
  }
}
